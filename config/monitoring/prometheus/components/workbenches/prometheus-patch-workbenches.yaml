---
# for prometheus container codeflare rules
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
spec:
  template:
    spec:
      containers:
        - name: prometheus
          volumeMounts:
            - mountPath: /etc/prometheus
              name: prometheus-workbenches-config
      volumes:
        - name: prometheus-workbenches-config
          configMap:
            name: prometheus-workbenches


