---
# for prometheus container codeflare rules
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
spec:
  template:
    spec:
      containers:
        - name: prometheus
          volumeMounts:
            - mountPath: /etc/prometheus
              name: prometheus-odh-model-controller-config
      volumes:
        - name: prometheus-odh-model-controller-config
          configMap:
            name: prometheus-odh-model-controller


